#ler um ângulo e mostrar sen cos tan

import math
ang = float(input('Ângulo: '))

print('sen({}) = {}'.format(ang, math.sin(ang)))
print('cos({}) = {}'.format(ang, math.cos(ang)))
print('tg ({}) = {}'.format(ang, math.tan(ang)))
